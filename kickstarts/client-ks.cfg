# Kickstart file automatically generated by anaconda.

install
cdrom
lang en_US.UTF-8
keyboard us
network --device eth0 --bootproto dhcp --netmask 255.0.0.0 --gateway 10.2.6.1 --nameserver 10.14.1.10,10.11.0.51 --hostname TA2Client
rootpw --iscrypted $1$rzMDBwLQ$WyZuAXPVicUW0Dtd873wO.
firewall --enabled --port=22:tcp
authconfig --enableshadow --enablemd5
selinux --enforcing
timezone --utc America/New_York
bootloader --location=mbr --driveorder=sda
# The following is the partition information you requested
# Note that any partitions you deleted are not expressed
# here so unless you clear all partitions first, this is
# not guaranteed to work
clearpart --linux
part / --fstype ext3 --size=7500
part swap --size=512
part /var --fstype ext3 --size=2000

reboot

%packages
@base
@core
@dialup
@editors
@text-internet
keyutils
iscsi-initiator-utils
trousers
fipscheck
device-mapper-multipath

%post --log=/root/ks.log

yum update -y
yum install ruby -y
yum install openldap-clients -y

authconfig --enableshadow --enableldap --enableldapauth --ldapserver=10.2.6.224 --ldapbasedn=dc=cit470_Team_4,dc=nku,dc=edu --update

echo "10.2.6.224:/home	/home			nfs	defaults	0 0" >> /etc/fstab

useradd -c "User 1" -s /bin/bash -d /home/user1 -p '$1$a1f8g7Ye$ebbmp.OtSJpycae15sk1v.' -M user1

%end
